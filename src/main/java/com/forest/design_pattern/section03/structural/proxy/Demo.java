package com.forest.design_pattern.section03.structural.proxy;

/**
 * An interface for accessing a particular resource.
 * Proxy provides the same interface but entirely different behavior.
 * Proxy: A class that functions as an interface to particular resource.
 *        That resource may be remote, expensive to construct, or may require logging or some other added functionality.
 *
 * Proxy VS Decorator
 *      Proxy provides an identical interface; decorator provides an enhanced interface
 *      Decorator typically aggregates what it is decorating; proxy doesn't have to
 */
public class Demo {

}
